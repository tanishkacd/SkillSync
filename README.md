# SkillSync

Database schema lives in database/SkillSync_Database_Schema.sql; runs automatically via Docker Compose, or manually with sqlcmd -i database/SkillSync_Database_Schema.sql against a local SQL Server instance.
